# Test Cases — CS→CS Project Dashboard

Source: feature.md, prd.md, trd.md (docs/features/cs-project-dashboard/)
Generated from: PRD/TRD only — no codebase or implementation was referenced.

Total: 117 test cases — 108 automated, 9 manual.

| Test Case ID | Module/Scenario | Test Type | Sub-Type | Test Case | Expected Result | Automated | Status |
|---|---|---|---|---|---|---|---|
| TC_PD_001 | Top bar (FR-1) | UI | Layout | Load the projects page and inspect the top bar. | The top bar renders three elements: a product mark, the product name, and a user avatar. It contains no organization block and no organization control. | Y | Not Run |
| TC_PD_002 | Top bar (FR-1) | UI | Product identity | Inspect the product name in the top bar. | It reads the literal string `Migrate to Contentstack`. | Y | Not Run |
| TC_PD_003 | Top bar (FR-1) | UI | Layout | Compare the projects page top bar against the migration wizard's app bar. | The projects page top bar is a distinct component: it carries a user avatar and contains no step-position indicator and no source indicator, even though both bars show the same product name. | Y | Not Run |
| TC_PD_004 | Top bar (FR-1) | UI | Avatar | Authenticate as a user whose first name is `Chirag` and last name is `Nair`, then inspect the avatar. | The avatar renders the two uppercase initials `CN`. | Y | Not Run |
| TC_PD_005 | Top bar (FR-1) | Functional | Avatar data source | Load the page and inspect which request supplies the user's name. | The name and email come from the dedicated user endpoint. No request is made to an organizations endpoint for this purpose. | Y | Not Run |
| TC_PD_006 | Top bar (FR-1) | Negative | Avatar fallback | Authenticate as a user with first name `Chirag` and no last name, then inspect the avatar. | The avatar renders the first two characters of the first name, uppercased: `CH`. | Y | Not Run |
| TC_PD_007 | Top bar (FR-1) | Negative | Avatar fallback | Authenticate as a user with no first name, no last name, and the email `chirag.chavan@example.com`, then inspect the avatar. | The avatar renders the first character of the email's local part, uppercased: `C`. | Y | Not Run |
| TC_PD_008 | Top bar (FR-1) | Negative | Avatar fallback | Authenticate as a user with no first name, no last name and no email, then inspect the avatar. | The avatar renders a person icon. | Y | Not Run |
| TC_PD_009 | Top bar (FR-1) | Negative | Avatar fallback | Across all four identity conditions (both names / one name / email only / nothing), inspect the avatar for empty content. | In no condition does the avatar render as an empty circle; every condition produces either characters or an icon. | Y | Not Run |
| TC_PD_010 | Top bar (FR-1) | Negative | User endpoint failure | Make the user endpoint fail, then load the page. | The avatar falls back to the person icon and the project list still loads and renders normally — the two requests are independent. | Y | Not Run |
| TC_PD_011 | Title row (FR-2) | UI | Heading | Load the projects page and inspect the page heading. | The literal heading `Migration Projects` is rendered. | Y | Not Run |
| TC_PD_012 | Title row (FR-2) | UI | Back control | Load the projects page and inspect the back control in the title row. | The back control is rendered in a disabled state. | Y | Not Run |
| TC_PD_013 | Title row (FR-2) | Negative | Back control | Activate the disabled back control with a pointer. | No navigation occurs and the projects page remains rendered. | Y | Not Run |
| TC_PD_014 | Title row (FR-2) | Accessibility | Back control | Tab through the title row from the page heading onward. | The disabled back control is not reachable in keyboard tab order. | Y | Not Run |
| TC_PD_015 | Title row (FR-2) | UI | Search field | Load the projects page with the search field empty and inspect its placeholder. | The placeholder reads the literal string `Search projects`. | Y | Not Run |
| TC_PD_016 | Title row (FR-2) | UI | Primary action | Load the projects page with at least one project and inspect the primary action's label. | The primary action's label reads the literal string `New Project`. | Y | Not Run |
| TC_PD_017 | Title row (FR-2) | Functional | Primary action | Activate the `New Project` control once. | The create-project modal opens directly; no intermediate dropdown or menu is presented. | Y | Not Run |
| TC_PD_018 | Title row (FR-2) | Negative | Primary action | Load the page for a user owning zero projects, and inspect the title row. | No control labelled `New Project` is present in the title row. | Y | Not Run |
| TC_PD_019 | Title row (FR-2) | Functional | Primary action | Load the page for a user owning exactly one project, and inspect the title row. | The control labelled `New Project` is present. | Y | Not Run |
| TC_PD_020 | Title row (FR-2) | Functional | Primary action | With 3 projects loaded, type a search term matching none of them. | The control labelled `New Project` remains present even though no card is visible. | Y | Not Run |
| TC_PD_021 | Project list (FR-3) | Functional | List rendering | With the caller owning 3 projects in their region, let the page finish loading. | Exactly 3 project cards are rendered, one per project. | Y | Not Run |
| TC_PD_022 | Project list (FR-3) | Functional | Request shape | Load the page and inspect the project list request that is issued. | Exactly one list request is issued, and it carries no organization path segment, query parameter or body field of any kind. | Y | Not Run |
| TC_PD_023 | Project list (FR-3) | Performance | Request count | Load the page, then trigger several re-renders that change no data. | Exactly one project list request has been issued in total; re-rendering issues no further requests. | Y | Not Run |
| TC_PD_024 | Project list (FR-3) | Negative | Soft-delete exclusion | With the store containing 4 of the caller's projects, one of which has its soft-delete marker set, let the page finish loading. | Exactly 3 project cards are rendered and the soft-deleted project is absent. | Y | Not Run |
| TC_PD_025 | Project list (FR-3) | Negative | Scope exclusion | With the store containing one project owned by a different user and one project created in a different region, let the page finish loading. | Neither of those two projects is rendered. | Y | Not Run |
| TC_PD_026 | Project list (FR-3) | Functional | State separation | Load 3 projects, then type a search term matching none of them, then clear the search. | The search shows the search-empty state (not the first-run state), and clearing the search restores all 3 cards — demonstrating the unfiltered list is retained separately from the filtered view. | Y | Not Run |
| TC_PD_027 | Project list (FR-3) | UI | Responsive grid | Load 8 projects and resize the viewport from a wide width down to a narrow one. | The grid reduces its column count as the width decreases, and no horizontal scrollbar appears on the page body at any width. | N | Not Run |
| TC_PD_028 | Project list (FR-3) | Functional | No pagination | Load a user owning 500 projects. | All 500 project cards are rendered and no pagination control is present. | Y | Not Run |
| TC_PD_029 | Project card (FR-4) | UI | Card content | Render a card for a project named `Blog content move`. | The card displays the project name `Blog content move`. | Y | Not Run |
| TC_PD_030 | Project card (FR-4) | UI | Card content | Render a card for a project named `Marketing stack sync`, last modified 10 days ago, with no persisted source and no persisted destination. | The card shows the name `Marketing stack sync`, the label `Source` with the value `Contentstack`, the label `Project Status` with the badge text `Draft`, and an absolute date in `MMM D, YYYY` form. | Y | Not Run |
| TC_PD_031 | Project card (FR-4) | UI | Date formatting | Render a card for a project last modified 3 hours before the reference time. | The footer reads `3 hours ago` and does not read an absolute date. | Y | Not Run |
| TC_PD_032 | Project card (FR-4) | UI | Date formatting | Render a card for a project last modified 10 days before the reference time of 25 May 2026. | The footer reads an absolute date in `MMM D, YYYY` form, for example `May 15, 2026`, and not a relative description. | Y | Not Run |
| TC_PD_033 | Project card (FR-4) | UI | Date formatting | Render cards for projects last modified 1 day, 2 days, 1 hour and 2 hours before the reference time. | The footers read `1 day ago`, `2 days ago`, `1 hour ago` and `2 hours ago` respectively — singular and plural forms are both correct. | Y | Not Run |
| TC_PD_034 | Project card (FR-4) | UI | Truncation | Render a card for a project whose name is longer than the card width. | The name is truncated visually only; the complete name string is still present in the document so it remains selectable and copyable. | Y | Not Run |
| TC_PD_035 | Project card (FR-4) | UI | Card content | Inspect the left-hand column of a rendered card. | The literal label `Source` is rendered above the literal value `Contentstack`. | Y | Not Run |
| TC_PD_036 | Project card (FR-4) | UI | Card content | Inspect the right-hand column of a rendered card. | The literal label `Project Status` is rendered above the status badge. | Y | Not Run |
| TC_PD_037 | Project card (FR-4) | UI | Card footer | Inspect the footer of a rendered card. | The footer contains a clock indicator and the project's last-modified time. | Y | Not Run |
| TC_PD_038 | Project card (FR-4) | Functional | Card activation | Inspect a rendered card for independently activatable regions, then activate the card anywhere within its bounds. | The card is a single activatable control; it contains no separately clickable sub-region, and activating any part of it produces the same single navigation. | Y | Not Run |
| TC_PD_039 | Project card (FR-4) | Functional | Resume step | Activate the card of project `P1`, which has a persisted destination. | The application navigates into the migration wizard for project `P1` at the Destination step. | Y | Not Run |
| TC_PD_040 | Project card (FR-4) | Functional | Resume step | Activate the card of project `P2`, which has neither a persisted source nor a persisted destination. | The application navigates into the migration wizard for project `P2` at the wizard's first step. | Y | Not Run |
| TC_PD_041 | Project card (FR-4) | Functional | Resume step | Activate the card of project `P3`, which has a persisted source whose last export succeeded and no persisted destination. | The application navigates into the migration wizard for project `P3` at the Audit step. | Y | Not Run |
| TC_PD_042 | Project card (FR-4) | Negative | Card activation | Activate a card whose project has an empty id. | No navigation occurs. | Y | Not Run |
| TC_PD_043 | Project card (FR-4) | Functional | Navigation target | Activate a project card and inspect the navigation target, then let the wizard mount. | The target identifies the project and the step only — it contains no organization segment and no organization query parameter — and the wizard's project-scoped reads are issued from the project id alone rather than being skipped. | Y | Not Run |
| TC_PD_044 | Project card (FR-4) | Accessibility | Keyboard | With 3 cards rendered, move focus with the keyboard to the second card and activate it with the Enter key. | The same navigation occurs as for a pointer click on that card. | Y | Not Run |
| TC_PD_045 | Project card (FR-4) | Security | Output encoding | Render a card for a project whose name contains markup-significant characters, for example `<img src=x onerror=alert(1)>`. | The name is rendered as literal visible text; no element is created from it and no script executes. | Y | Not Run |
| TC_PD_046 | Status badge (FR-5) | Functional | Status derivation | Render a card for a project with neither a persisted source nor a persisted destination. | The badge renders the literal text `Draft`. | Y | Not Run |
| TC_PD_047 | Status badge (FR-5) | Functional | Status derivation | Render a card for a project with a persisted source and no persisted destination, which has not completed a migration. | The badge renders the literal text `In Progress`. | Y | Not Run |
| TC_PD_048 | Status badge (FR-5) | Functional | Status derivation | Render a card for a project with a persisted destination, which has not completed a migration. | The badge renders the literal text `In Progress`. | Y | Not Run |
| TC_PD_049 | Status badge (FR-5) | Negative | Status derivation | Supply a project record carrying a stored status value that contradicts its documents — for example a stored `Completed` with no persisted source or destination. | The badge reads `Draft`, derived from the documents; the stored value is not read and does not appear anywhere on the card. | Y | Not Run |
| TC_PD_050 | Status badge (FR-5) | UI | Status badge | Render one card in each reachable status and compare the badges. | Each status renders an icon alongside its text, and the icon differs per status. | Y | Not Run |
| TC_PD_051 | Status badge (FR-5) | Functional | Status derivation | Supply the status derivation with a project that has completed a migration. | The derivation returns the literal text `Completed`. | Y | Not Run |
| TC_PD_052 | Status badge (FR-5) | Negative | Status badge | Supply a project whose derived status cannot be determined from its documents. | The badge renders `Draft`; it is neither empty nor absent. | Y | Not Run |
| TC_PD_053 | Status badge (FR-5) | Accessibility | Status badge | Inspect the status badge with colour information disregarded. | The status is conveyed by the badge's text, so the meaning survives without colour. | Y | Not Run |
| TC_PD_054 | Search (FR-6) | Functional | Filter | With `Marketing stack sync`, `Docs stack copy` and `Blog content move` loaded, type `stack` into the search field. | Exactly 2 cards are rendered — `Marketing stack sync` and `Docs stack copy` — and no network request is issued. | Y | Not Run |
| TC_PD_055 | Search (FR-6) | Functional | Filter | With `Marketing stack sync` loaded, type `MARKETING` into the search field. | The `Marketing stack sync` card is rendered, demonstrating the match is case-insensitive. | Y | Not Run |
| TC_PD_056 | Search (FR-6) | Negative | Filter | Load a project named `Blog content move` whose description contains the word `quarterly`, then search for `quarterly`. | No card is rendered, because the filter matches the project name only and not the description. | Y | Not Run |
| TC_PD_057 | Search (FR-6) | Functional | Filter | With a search for `stack` filtering the list to 2 of 3 projects, clear the search field. | All 3 cards are rendered again. | Y | Not Run |
| TC_PD_058 | Search (FR-6) | Functional | URL seeding | Open the page at a URL whose query string contains `search=blog`. | The search field contains `blog` and the grid is filtered to matching projects only. | Y | Not Run |
| TC_PD_059 | Search (FR-6) | Functional | URL seeding | Open the page at a URL whose query string contains `search=%20blog%20` (the term surrounded by spaces). | The search field contains `blog` with the surrounding whitespace trimmed, and the grid is filtered accordingly. | Y | Not Run |
| TC_PD_060 | Search (FR-6) | Negative | Filter | With 3 projects loaded, type `zzzz` into the search field. | No cards are rendered, the heading `No projects match your search` is visible, and the first-run empty state is not visible. | Y | Not Run |
| TC_PD_061 | Search (FR-6) | Negative | Filter | With 3 projects loaded, type three space characters into the search field. | All 3 cards are rendered and the search-empty state is not shown — a whitespace-only search is treated as an empty search. | Y | Not Run |
| TC_PD_062 | Search (FR-6) | UI | Clear control | Type `stack` into the search field and inspect the field. | A visible control to clear the field's value is present. | Y | Not Run |
| TC_PD_063 | Search (FR-6) | UI | Clear control | Load the page with the search field empty and inspect the field. | No clear control is offered while the field is empty. | Y | Not Run |
| TC_PD_064 | Create project (FR-7) | Functional | Modal | With at least one project loaded, activate the control labelled `New Project`. | A modal opens containing a name field and a description field. | Y | Not Run |
| TC_PD_065 | Create project (FR-7) | Negative | Validation | With the create modal open and the name field empty, attempt to submit. | No create request is issued and the modal stays open. | Y | Not Run |
| TC_PD_066 | Create project (FR-7) | Negative | Validation | With the create modal open, enter a 201-character name and attempt to submit. | The name is rejected, a validation message is shown, and no create request is issued. | Y | Not Run |
| TC_PD_067 | Create project (FR-7) | Functional | Validation boundary | With the create modal open, enter a name of exactly 200 characters and submit. | The name is accepted and a create request is issued. | Y | Not Run |
| TC_PD_068 | Create project (FR-7) | Negative | Validation | With the create modal open, enter the name ` Leading space` and attempt to submit. | The name is rejected and no create request is issued. | Y | Not Run |
| TC_PD_069 | Create project (FR-7) | Functional | Validation | With the create modal open, enter a valid name, leave the description empty, and submit. | The submission is accepted and a project is created — the description is optional. | Y | Not Run |
| TC_PD_070 | Create project (FR-7) | Negative | Validation | With the create modal open and a valid name entered, enter a 256-character description and attempt to submit. | The description is rejected and no create request is issued. | Y | Not Run |
| TC_PD_071 | Create project (FR-7) | Functional | Validation boundary | With the create modal open and a valid name entered, enter a description of exactly 255 characters and submit. | The description is accepted and a create request is issued. | Y | Not Run |
| TC_PD_072 | Create project (FR-7) | Negative | Submit gating | With the create modal open and the name field invalid, inspect and activate the submit control. | The submit control is inoperative and activating it issues no create request. | Y | Not Run |
| TC_PD_073 | Create project (FR-7) | Security | Request body | With the create modal open, enter the name `EU region migration` and no description, then submit and inspect the request body. | The request body contains only the name and the description; it contains no project id, owner or region. | Y | Not Run |
| TC_PD_074 | Create project (FR-7) | Functional | Post-create navigation | Submit the create modal and let the server respond with a created project of id `P9`. | Leftover wizard state from any previously open project is discarded, and then the application navigates into the migration wizard for project `P9` at its first step, in that order. | Y | Not Run |
| TC_PD_075 | Create project (FR-7) | Negative | Cancel | With the create modal open and a valid name entered, activate the modal's cancel control. | No create request is issued and the project list is unchanged. | Y | Not Run |
| TC_PD_076 | Create project (FR-7) | Negative | Cancel | With the create modal open and a valid name entered, activate the modal's close control. | No create request is issued and the project list is unchanged. | Y | Not Run |
| TC_PD_077 | Create project (FR-7) | Negative | Cancel | With the create modal open and a valid name entered, activate the modal's overlay. | No create request is issued and the project list is unchanged. | Y | Not Run |
| TC_PD_078 | Create project (FR-7) | Negative | Concurrency | With the create modal open and a valid name entered, activate the submit control twice in rapid succession while the first request is still in flight. | Exactly one create request has been issued in total. | Y | Not Run |
| TC_PD_079 | Create project (FR-7) | Negative | Failure handling | With the create modal open and the name `EU region migration` entered, make the create request fail. | The modal remains open, the failure is shown, and the name field still contains `EU region migration`. | Y | Not Run |
| TC_PD_080 | Create project (FR-7) | Usability | Modal presentation | Open the create-project modal and compare its presentation with the other v3 modals in the product. | The modal's overlay, panel, spacing, field and button treatment are consistent with the existing v3 modals, so it does not read as a differently-designed dialog. | N | Not Run |
| TC_PD_081 | Create project (FR-7) | Security | Server-side validation | Bypass the client and send a create request directly with a 201-character name. | The server rejects the request with a 400 response and creates no project — client validation is an affordance, not the enforcement point. | Y | Not Run |
| TC_PD_082 | Page states (FR-8) | UI | Loading | Request the projects page and inspect the grid before the response arrives. | Placeholder cards are visible, no real project card is visible, and no empty state is visible. | Y | Not Run |
| TC_PD_083 | Page states (FR-8) | Usability | Loading | Watch the grid as placeholder cards are replaced by real project cards. | The placeholders occupy the same grid positions and approximately the same dimensions as the real cards, so the layout does not visibly shift when the real cards arrive. | N | Not Run |
| TC_PD_084 | Page states (FR-8) | Negative | Loading | While placeholder cards are visible, attempt to activate one. | Nothing happens; the placeholder is not activatable and no navigation occurs. | Y | Not Run |
| TC_PD_085 | Page states (FR-8) | Functional | First-run empty state | Load the page for a user owning zero projects and let the request succeed. | The first-run empty state is visible and the search-empty state is not. | Y | Not Run |
| TC_PD_086 | Page states (FR-8) | Functional | First-run empty state | With the first-run empty state visible, activate its create control. | The same create-project modal opens as the one opened by `New Project`. | Y | Not Run |
| TC_PD_087 | Page states (FR-8) | UI | Search-empty state | With projects loaded, search for a term matching none of them, then read the empty-state text. | The heading reads the literal string `No projects match your search` and the body reads the literal string `Try a different name or clear the search.` | Y | Not Run |
| TC_PD_088 | Page states (FR-8) | Negative | State exclusivity | With a search active and matching no project, inspect the page for the first-run empty state. | The first-run empty state is not shown while a search is active. | Y | Not Run |
| TC_PD_089 | Page states (FR-8) | Negative | State exclusivity | With the search field empty and zero projects loaded, inspect the page for the search-empty state. | The search-empty state is not shown while the search field is empty. | Y | Not Run |
| TC_PD_090 | Page states (FR-8) | Negative | Error state | Make the project list request fail with a 500 response and let the page settle. | An error state with a retry control is visible; no placeholder cards remain; and neither empty state is visible. | Y | Not Run |
| TC_PD_091 | Page states (FR-8) | Functional | Error recovery | With the error state visible after a failed request, activate retry and let the retried request succeed with 2 projects. | 2 project cards are rendered and the error state is gone, with no page reload having occurred. | Y | Not Run |
| TC_PD_092 | Page states (FR-8) | Negative | Error state | Make the project list request fail with a network error, and separately with a timeout. | In both cases the error state with a retry control is rendered, and in neither case is an empty list rendered. | Y | Not Run |
| TC_PD_093 | Page states (FR-8) | Negative | Session expiry | Make the project list request fail with a 401 response and let the page settle. | This page renders neither an error state nor an empty state; the shared session handling clears the session and redirects to the login entry. | Y | Not Run |
| TC_PD_094 | Data & scoping (FR-9) | Functional | Record shape | Create a project named `EU region migration` and inspect the persisted record. | The record carries the name `EU region migration`, the caller's region, the caller's user id as owner, an unset soft-delete marker, and both created and updated timestamps. | Y | Not Run |
| TC_PD_095 | Data & scoping (FR-9) | Functional | Record shape | Create a project supplying no description, and inspect the persisted record. | The record is valid without a description — the field is optional. | Y | Not Run |
| TC_PD_096 | Data & scoping (FR-9) | Negative | Record shape | Inspect a newly created project record for a stored status field. | The record carries no stored status field; status exists only as a derived value. | Y | Not Run |
| TC_PD_097 | Data & scoping (FR-9) | Negative | Record shape | Inspect a newly created project record for any organization field, and attempt to create a project supplying one. | The record carries no organization field under any name, and an organization supplied by a caller is not written onto it. | Y | Not Run |
| TC_PD_098 | Data & scoping (FR-9) | Negative | Required fields | Attempt to bring a project record into existence without a name, without a region or without an owner — including via the side effect of persisting a source document against a project id that does not yet exist. | No such record is created; all three fields are required on every creation path. | Y | Not Run |
| TC_PD_099 | Data & scoping (FR-9) | Security | List scope | Request the project list as a caller in region `NA` with user id `U1`, against a store also containing projects in another region, with another owner, and one soft-deleted. | Only records matching all three of region `NA`, owner `U1` and not soft-deleted are returned. | Y | Not Run |
| TC_PD_100 | Data & scoping (FR-9) | Security | Single-read scope | Request project `P4`, which is owned by a different user, directly by its id. | The response is not found; it does not contain the project and it does not indicate that the project exists. | Y | Not Run |
| TC_PD_101 | Data & scoping (FR-9) | Security | Single-read scope | Request project `P5`, which is owned by the caller but was created in a different region, directly by its id. | The response is not found, matching the fact that the list also excludes it. | Y | Not Run |
| TC_PD_102 | Data & scoping (FR-9) | Security | Single-read scope | Request a soft-deleted project directly by its id. | The response is not found. | Y | Not Run |
| TC_PD_103 | Data & scoping (FR-9) | Security | Information disclosure | Compare the response for a project id that exists but fails the scope with the response for a project id that does not exist at all. | Both responses are not found and are indistinguishable; neither confirms that a project with that id exists. | Y | Not Run |
| TC_PD_104 | Data & scoping (FR-9) | Negative | Legacy records | Place a project record with no name, no region and no owner in the store, then list projects and also request that record directly by its id. | The record does not appear in the list and is not readable by its id; the scope is not relaxed to accommodate it. | Y | Not Run |
| TC_PD_105 | Data & scoping (FR-9) | Security | Authentication | Send a project list request with no session token, and again with an invalid session token. | Both requests are rejected as unauthorized and no project data is returned. | Y | Not Run |
| TC_PD_106 | Data & scoping (FR-9) | Security | Authentication | Send a project create request with no session token, and again with an invalid session token. | Both requests are rejected as unauthorized and no project is created. | Y | Not Run |
| TC_PD_107 | Data & scoping (FR-9) | Security | Identity source | Send a project list request whose body and query string contain a region and a user id different from those in the session token. | The region and user id used for scoping are taken from the token; the values supplied in the body and query string are ignored. | Y | Not Run |
| TC_PD_108 | Data & scoping (FR-9) | Functional | Endpoint paths | Request each project-scoped endpoint at its new path, then request each one at its former `/v3/org/:orgId/...` path. | Every new path resolves. Every former path returns not found, so no organization-scoped mount remains. | Y | Not Run |
| TC_PD_109 | Non-functional (NFR) | Performance | Search responsiveness | With 200 projects loaded, type a single character into the search field and measure the time from keystroke to committed render on a mid-range laptop. | The rendered grid updates within 100 ms. | N | Not Run |
| TC_PD_110 | Non-functional (NFR) | Accessibility | Keyboard operability | Using the keyboard only, reach and operate every interactive control on the page: the search field and its clear control, the primary action, each project card, and every control in the create modal. | Every control is reachable and operable by keyboard alone and exposes an accessible name. | Y | Not Run |
| TC_PD_111 | Non-functional (NFR) | Accessibility | Focus visibility | Tab through every interactive control on the page and observe the focus indicator. | Each focused control shows a visible focus indicator, meeting WCAG 2.1 AA. | N | Not Run |
| TC_PD_112 | Non-functional (NFR) | Security | Logging | Create a project, then inspect the server log entry for the creation. | The log entry records the new project id. | Y | Not Run |
| TC_PD_113 | Non-functional (NFR) | Security | Logging | Create a project named `EU region migration` with the description `Confidential customer note`, then search the server logs for both strings. | Neither the project name nor the description appears anywhere in the logs. | Y | Not Run |
| TC_PD_114 | Non-functional (NFR) | Compatibility | Browser rendering | Load the projects page in each browser the v3 UI supports and compare the rendering. | The page renders correctly in every supported browser, with no layout breakage in the top bar, the title row, the grid or the modal. | N | Not Run |
| TC_PD_115 | Non-functional (NFR) | Usability | Empty-state clarity | Compare the first-run empty state with the search-empty state as a first-time user would encounter them. | The two states are clearly distinguishable, each explains the situation it represents, and the first-run state offers an obvious way to create a first project. | N | Not Run |
| TC_PD_116 | Non-functional (NFR) | Usability | Disabled control clarity | Present the page to a user unfamiliar with it and observe their reaction to the disabled back control. | The user does not attempt to use the back control repeatedly and does not report it as a broken feature. | N | Not Run |
| TC_PD_117 | Non-functional (NFR) | Usability | Top bar deviation | Show the top bar to the design owner alongside the design's own top bar, which carries an organization block. | The design owner confirms the product-name substitute is acceptable, or specifies a different treatment. | N | Not Run |

## Gaps / Needs Clarification

- **EC-6 / Q-13 / TQ-1 — duplicate project names.** feature.md EC-6 says the behaviour on a name conflict "depends on whether duplicate names are permitted at all", and trd.md TQ-1 leaves that undecided. No test case can state an expected result for submitting a name that already exists.
- **Q-1 / FR-8.9 — first-run empty state copy and placeholder card treatment.** FR-8.3 requires a first-run empty state but specifies none of its text, and FR-8.1 specifies the placeholders' footprint but not their visual treatment. The cases above therefore assert only presence, footprint and behaviour — not copy. Once copy is signed off, the corresponding cases need a literal-string assertion added.
- **Q-3 / FR-7.14 — create-project modal design.** The requirement is that the modal "follow the existing v3 modals", which is a direction rather than a checkable specification. The single case covering it is necessarily a manual comparison with no pass/fail threshold.
- **Q-15 — project card hover state.** The design declares a transition on the card's border colour, shadow and transform but specifies no hover values. No test case can assert what the card should look like on hover.
- **Q-16 / FR-1.1 — the top bar's substitute for the organization block.** The design's top bar is built around an organization block this revision removes. FR-1.1 substitutes the product name, but that is a drafted deviation, not a specified design, so the only case covering it is a manual design review.
- **Q-4 — no `Failed` status.** FR-5.5 defines a `Completed` badge, and prd.md PR-1 acknowledges that a migration can fail, but no requirement defines a `Failed` badge, its text or its derivation. No case exists for a failed project.
- **FR-5.5 — `Completed` is unreachable end-to-end.** The requirement states that no project can currently reach this state because v3 has no migration-completion signal. The case above therefore exercises the derivation directly with a completed input; it verifies the derivation rather than the user-visible outcome.
- **FR-4.9 / Q-2 — resume step for steps that persist nothing.** The resume step is derived from the persisted source and destination only. No requirement states what should happen for a user who reached Content mapping, Preview, Migrate or Verify without persisting anything. Cases exist only for the three derivable conditions.
- **Q-10 / EC-15 / TQ-5 — pagination threshold.** "All projects rendered, no pagination" is testable and is covered, but the project count at which that becomes unacceptable is explicitly undefined, so no threshold case exists.
- **Q-12 / NFR-8 — supported browsers.** NFR-8 requires correct rendering in "the browsers already supported by the v3 UI" and marks the exact versions TBD. The compatibility case above cannot name the browsers it should be run against.
- **TQ-2 — server-side create idempotency.** trd.md states API-2 is deliberately not idempotent, so two identical requests create two projects, while also recording as open whether an idempotency key should exist. The concurrency case above covers only the client-side guard.
- **TQ-3 — user identity caching.** Whether the user endpoint is called on every page mount or cached for the session is undecided. This changes the expected request count, so the request-count case above deliberately covers only the project list request.
- **TQ-4 / NFR-1 — how the 100 ms bar is measured.** trd.md records that a wall-clock assertion for this threshold is unreliable in continuous integration and leaves the measurement approach open. The performance case above is therefore marked as not automated.
- **FR-3.5 — responsive grid has no stated breakpoints.** The requirement is that the grid reflows "without a horizontal scrollbar appearing on the page body", but no breakpoint set and no minimum supported width are specified, so the case cannot enumerate the widths to test and is marked manual.
- **FR-1.6 — "not the wizard chrome's app bar component" is architectural.** It constrains how the page is built rather than what it does, and the two bars now show the same product name. The case above tests it only indirectly, by asserting the projects bar carries an avatar and no step-position or source indicator.
- **Q-6 — nothing sets the soft-delete marker.** FR-9.5 requires the marker and its exclusion from listings, but no delete action exists anywhere in the product. The exclusion cases above can only be exercised by placing an already-marked record in the store directly, never through a user action.
- **PQ-3 — description is collected and displayed nowhere.** The create modal collects a description and the record stores it, but no requirement states where it is ever shown. No display case exists for it.
- **PQ-5 — irreversibility warning in the create modal.** prd.md records as undecided whether the create modal should warn that a project cannot be deleted. No case asserts the presence or absence of such a warning.
- **FR-9.13 — the former paths' 404 depends on nothing else re-registering them.** The endpoint-path case asserts the old `/v3/org/:orgId/...` paths no longer resolve, which is a check against a stale mount. It cannot detect a future feature legitimately reintroducing an organization-scoped route for a different purpose.
